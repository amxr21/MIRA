# Core vision
opencv-python>=4.8
numpy>=1.24

# Model inference (CPU/ONNX fallback)
onnxruntime>=1.16

# Arduino serial interface
pyserial>=3.5

# Manual keyboard control
pynput>=1.7

# Web dashboard (streaming/, future use)
flask>=3.0
flask-socketio>=5.3

# Hailo NPU SDK — install separately on Raspberry Pi + Hailo-8L
# See: https://github.com/hailo-ai/hailort
# hailo_platform
